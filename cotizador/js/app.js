/* const input = document.getElementById('input');
const check = document.getElementById('check');

const handleCheckFocus = () => {
    if (check.classList.contains('invisible')) {
        check.classList.remove('invisible');
        check.classList.add('visible');
    }
};

const handleCheckBlur = () => {
    if (check.classList.contains('visible')) {
        check.classList.remove('visible');
        check.classList.add('invisible');
    }
};

const start = () => {
    input.addEventListener('focus', handleCheckFocus);
    input.addEventListener('blur', handleCheckBlur);
};

window.onload = start; */
